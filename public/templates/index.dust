{>"layouts/master" /}

{<body}
  {#books}
    <div class="col-lg-3 book">
      <img class="imageframe" src="/images/{.cover}">
      <h4>{.title}</h4>
      <p>{.truncText}</p>
      <div class="price">Buy it for <span>${.price}</span></div>
      <br>
      <a href="/books/details/{.id}" class="btn btn-primary">Book Details</a>
    </div>
  {/books}
{/body}
