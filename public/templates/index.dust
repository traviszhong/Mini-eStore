{>"layouts/master" /}

{<body}
  {#songs}
    <div class="col-lg-3 song">
      <img class="imageframe" src="/images/{.style}.jpg">
      <h4>{.title}</h4>
      <p>{.truncText}</p>
      <div class="price"><span>${.price}</span></div>
      <br>
      <a href="/songs/details/{.id}" class="btn btn-primary">Song Details</a>
    </div>
  {/songs}
{/body}
